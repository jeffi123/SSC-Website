import { defineComponent } from "vue";
const componentName = "IContainer";
export default defineComponent({
  name: componentName,
  props: {
    fluid: {
      type: Boolean,
      default: false
    }
  },
  computed: {
    classes() {
      return {
        "-fluid": this.fluid
      };
    }
  }
});
